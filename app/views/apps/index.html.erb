<h2>Your Apps</h2>
<ul>
<% @apps.each do |app|%>
  <li><%= link_to app.title, app.url %>
  (<%= link_to "edit", edit_app_path(app) %>)
  </li>
<% end %>
</ul>
<p>
  <%= link_to "add new app", new_app_path %>
</p>
